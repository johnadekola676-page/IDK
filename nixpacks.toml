[phases.setup]
nixPkgs = ["nodejs_22", "git", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run init-db"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
