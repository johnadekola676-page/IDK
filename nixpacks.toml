[phases.setup]
nixPkgs = ["nodejs_22", "git"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npm run init-db",
  "npx playwright install --with-deps chromium"
]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
