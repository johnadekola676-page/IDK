# Frontend Configuration
# These variables are embedded at build time

# API URL (leave empty to use same origin as frontend)
VITE_API_URL=

# WebSocket URL (leave empty to use same origin as frontend)
VITE_WS_URL=

# Authentication token for API requests (optional, only needed in production)
VITE_API_TOKEN=
